function sayHi(name: string): void {
    console.log(`Hello ${name}`);
}

let person = "Piter";

type Student = {
    name: string;
    age: number;
};

const newStudent: Student = {
    name: "Slava",
    age: 25,
};
